def regexPipperGroup():
    print("""
    1. Groups are created in regex strings with parenthesis
    2. The set of parenthesis is group1 , the second is group 2 and so on.
    3.Calling group or group(0) returns the full matching string, group1 returns group 1's matching string and so on.
    4. Use \(and \) to match literal parenthesis in the regex string
    5. The pipe | can match one of the possible groups

    """)

regexPipperGroup()